---
name: dazangjing
description: 大正藏/佛经本地经库检索技能。当用户说"查佛经"、"这句出自哪部经"、"大正藏里怎么讲"、"查原文"、"《XX经》原文"、"问佛经"时触发。从本地 CBETA 经库（5017部经全文）逐字检索原文并引用，注明出处（部类/经号/经名），再讲义理。严禁凭记忆编造引文，引文必须先经检索验证。
---

# 大正藏本地经库技能

## 经库概况

- 位置：环境变量 `CBETA_HOME` 指定的目录（默认 `~/佛经库/`）
- 数据源：CBETA 官方 GitHub 开源数据（cbeta-org/xml-p5，繁体中文，繁体原样保存）
- 规模：**5017 部经**全文，含大正藏(T)、卍续藏(X)、嘉兴藏(J)、高丽藏(K)、南传大藏经(N)、太虚全书(YP)、印顺著作(Y)等 26 个部类
- 目录文件：`$CBETA_HOME/catalog.tsv`（部类/经号/经名/译者/文件路径）
- 原始 XML 底本：`$CBETA_HOME/cbeta-xml/`（需要校勘、异文时用）
- 生僻字对照：`$CBETA_HOME/工具/cbeta_gaiji/cbeta_gaiji.csv`

## 检索方法（必须先检索，再回答）

**1. 全文检索原文词句：**
```bash
python3 scripts/search.py "應無所住而生其心"
```
输出命中经名 + 原文上下文。

**2. 按经名查目录：**
```bash
python3 scripts/search.py 金剛經 --name
```
拿到文件路径后直接 Read 该文件看全文。

**3. 直接 grep（备用）：**
```bash
grep -rlF "检索词" "$CBETA_HOME/经文/T" --include="*.txt"
```

## 回答规范

1. **引文必须逐字来自检索结果**，注明出处：《经名》（部类+经号，如 T08n0236a）。没检索到就说没有，不凭记忆硬背——记忆可能有偏差，检索不会。
2. 用户报的句子若是口语/简体/意译，先转成文言繁体的可能形态再检索（如"无所住"→"應無所住"），多试几个变体。
3. 检索到原文后，可再讲义理、各家注疏立场（天台/贤首/禅宗等），分清"经文原文"与"后人注解"。
4. 经文是繁体，回答时可根据用户习惯用简体解释，但**引文保持繁体原样**。
5. 同名经有多个译本（如《心經》有 T0250/T0251 等），引用时注明是哪个译本。
6. 用户问"X经怎么说的"但该经检索不到相关句子时，如实说明"该经未见此语"，可补充出自他经的相近原文。

## 注意

- 经库为繁体中文，检索词要用繁体（可从上下文猜常用佛典用字）。
- 大部头经（如《華嚴經》八十卷）单文件较大，用 grep -n 定位行号再 sed 截取，别整个读入。
- 校勘记、脚注已在转文本时去除，需要异文校勘时去 `cbeta-xml/` 查原始 XML。
- 数据版本：CBETA XML P5（2026-09 克隆）。更新方法：`cd $CBETA_HOME/cbeta-xml && git pull` 后重跑 `scripts/convert.py`。
